const btn=document.querySelector('.menu-btn');const nav=document.querySelector('nav');btn?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const slides=[...document.querySelectorAll('.presentation-slide')];let current=0;const range=document.querySelector('#pres-range');const counter=document.querySelector('#pres-current');function showSlide(n){if(!slides.length)return;current=Math.max(0,Math.min(slides.length-1,n));slides.forEach((s,i)=>s.classList.toggle('active',i===current));if(range)range.value=current+1;if(counter)counter.textContent=current+1}document.querySelector('#pres-prev')?.addEventListener('click',()=>showSlide(current-1));document.querySelector('#pres-next')?.addEventListener('click',()=>showSlide(current+1));document.querySelector('.pres-nav.prev')?.addEventListener('click',()=>showSlide(current-1));document.querySelector('.pres-nav.next')?.addEventListener('click',()=>showSlide(current+1));range?.addEventListener('input',e=>showSlide(+e.target.value-1));document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft')showSlide(current-1);if(e.key==='ArrowRight')showSlide(current+1)});showSlide(0);